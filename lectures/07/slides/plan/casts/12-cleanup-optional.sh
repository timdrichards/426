#!/usr/bin/env bash
set -euo pipefail

# Record with:
# asciinema rec -q -c "bash --noprofile --norc" ../../public/casts/12-cleanup-optional.cast

# Commands to run during recording (use with care):
# docker container prune
# docker image prune
# docker system prune -a
